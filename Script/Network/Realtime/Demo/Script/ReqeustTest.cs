using UnityEngine;

public class ReqeustTest : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Alpha2))
        {
            //Single.RealTime.GetServerInfoTest(true, "Main", ( datas ) =>
            //{
            //    for (int i = 0; i < datas.Count; i++)
            //    {
            //        Debug.Log(datas[i].RoomId);
            //    }
            //});
        }

    }

}
